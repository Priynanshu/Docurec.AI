// Email utility — not used in simple auth mode
// Kept as empty stubs to avoid import errors if referenced anywhere

const sendOTPEmail = async () => {};
const sendWelcomeEmail = async () => {};

module.exports = { sendOTPEmail, sendWelcomeEmail };
